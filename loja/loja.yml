spring:
  datasource:
    password: root
    username: root
    url: jdbc:mysql://localhost:3306/loja
  jpa:
    database-platform: org.hibernate.dialect.MySQL8Dialect
    hibernate:
      ddl-auto: update
